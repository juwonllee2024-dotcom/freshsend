# FreshSend verification record — 2026-09-23

## Product path

- Candidate: `examples/demo-workspace/outgoing/report.txt`
- Explicit comparison root: `examples/demo-workspace`
- Observed result: JSON `status: "stale"`, one newer same-name path, and SHA-256 values for both files.
- Safety behavior: no upload, network call, subprocess, background watcher, or file mutation.

## Fresh commands

These commands were run again before the follow-up public commit:

```text
python -m pytest -q
ruff check .
mypy src
python -m build
pip-audit --path dist
git diff --check
```

## Security review

- Codex Security scan: `b933ecdd-13f9-4dd0-8914-fa7b52531e9f`
- Mode: standard repository scan
- Coverage: complete
- Findings: 0
- The scan reviewed the source snapshot before this documentation-only record was added; no production code changed after the scan.
- Delegated workers were unavailable, so the scan ran as a single-thread prompt-driven review.

## Remote delivery

- First CI run: failed on Ubuntu because the symlink fixture placed its target inside the comparison root; the test, not the product behavior, was wrong.
- Follow-up fix: moved the symlink target outside the explicit comparison root and reran the local suite.
- Follow-up commit and CI run are the release gate; a release is not claimed until that run is green.

## Known limitation

Windows environments without symlink creation permission skip the two symlink tests. The implementation still rejects a symlink candidate and skips symlink matches when the operating system exposes them.
